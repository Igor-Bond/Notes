/**
 * Версия приложения — одно число на весь проект.
 *
 * Показывается внизу экрана выбора. Не путать с APP_VERSION в sw.js: та
 * отвечает за сброс кэша и поднимается при любом изменении файлов, даже
 * невидимом.
 */
export const VERSION = '1.1.0';
